---
- name: Restart docker
  ansible.builtin.systemd_service:
    name: docker
    state: restarted
    daemon-reload: true
    enabled: true
  become: true
  when: system_systemd_managed
  listen: "docker : restart"
